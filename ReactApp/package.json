{
  "name": "my-app",
  "version": "0.1.0",
  "homepage": "https://kmchmk.github.io/SimplestGameEver",
  "private": true,
  "dependencies": {
    "@apollo/client": "^3.6.9",
    "@auth0/auth0-react": "^1.12.0",
    "graphql": "^16.6.0",
    "graphql-ws": "^5.11.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^2.1.3",
    "eslint-plugin-import": "^2.26.0",
    "subscriptions-transport-ws": "^0.11.0"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^4.0.0"
  }
}